name: Build Safe iTLS-Enso

on:
  push:
    branches: [ "main", "master", "main-uma0" ]
  pull_request:
    branches: [ "main", "master", "main-uma0" ]
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: vitasdk/vitasdk:latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Build Plugin
        run: |
          make

      - name: Locate compiled file (Debug)
        run: find . -name "*.skprx"

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
            name: safe-itls-enso-plugin-${{ github.ref_name }}
            path: |
              *.skprx
              */*.skprx
